{block content}
    <h2>Upravit místnost: {$room->roomNumber}</h2>

    <!-- Formulář pro editaci místnosti -->
    {control roomForm}
{/block}

{block scripts}
<script>
    // Rozšíření Nette AJAX pro přesměrování po odeslání formuláře
    $.nette.ext('redirect', {
        success: function (payload) {
            if (payload.redirect) {
                window.location.href = payload.redirect; // Přesměrování na danou URL
            }
        }
    });
</script>
{/block}